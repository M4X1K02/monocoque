name: installer

on:
  workflow_dispatch:
  pull_request:
    paths:
      - install.sh
      - tools/uninstall.sh
      - tools/monocoque-manager
      - tools/distro/**
      - tests/startup_play.sh
      - tests/ensure_simd_test.c
      - src/monocoque/helper/ensure_simd.c
      - src/monocoque/helper/ensure_simd.h
      - README.md
      - HOW-TO-USE.md
      - udev/**
      - conf/monocoque.config
      - .github/workflows/installer.yml
  push:
    paths:
      - install.sh
      - tools/uninstall.sh
      - tools/monocoque-manager
      - tools/distro/**
      - tests/startup_play.sh
      - tests/ensure_simd_test.c
      - src/monocoque/helper/ensure_simd.c
      - src/monocoque/helper/ensure_simd.h
      - README.md
      - HOW-TO-USE.md
      - udev/**
      - conf/monocoque.config
      - .github/workflows/installer.yml

permissions:
  contents: read

# TMPDIR is what the test script uses to place LOGDIR when INSTALLER_LOG_DIR is unset.
env:
  TMPDIR: ${{ runner.temp }}
  INSTALLER_LOG_DIR: ${{ runner.temp }}/monocoque-install-tests

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax
        run: bash tools/distro/test-install-containers.sh syntax

  detect:
    needs: syntax
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        name: [archlinux, fedora, ubuntu, debian]
    steps:
      - uses: actions/checkout@v4
      - name: Distro family detection
        run: bash tools/distro/test-install-containers.sh detect ${{ matrix.name }}
      - name: Upload container logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: installer-detect-${{ matrix.name }}
          path: ${{ env.INSTALLER_LOG_DIR }}
          if-no-files-found: warn
          retention-days: 7

  detect-edge:
    needs: syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Nobara / Mint / Bazzite os-release mocks
        run: bash tools/distro/test-install-containers.sh mocks
      - name: Immutable distros refuse host install
        run: bash tools/distro/test-install-containers.sh immutable
      - name: Upload container logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: installer-detect-edge
          path: ${{ env.INSTALLER_LOG_DIR }}
          if-no-files-found: warn
          retention-days: 7

  install:
    needs: syntax
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        name: [archlinux, fedora, ubuntu]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Full source install (${{ matrix.name }})
        run: bash tools/distro/test-install-containers.sh full ${{ matrix.name }}
      - name: Installer log on failure
        if: failure()
        run: |
          log="${INSTALLER_LOG_DIR}/${{ matrix.name }}.full.log"
          if [ -f "$log" ]; then
            tail -n 120 "$log"
          fi
      - name: Upload installer logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: installer-full-${{ matrix.name }}
          path: ${{ env.INSTALLER_LOG_DIR }}
          if-no-files-found: warn
          retention-days: 7
